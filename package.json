{
	"private": true,
	"scripts": {
		"build": "pnpm --dir FrontEnd install --frozen-lockfile && pnpm --dir FrontEnd run build"
	},
	"dependencies": {
		"@tanstack/react-query": "^5.90.12",
		"clsx": "^2.1.1",
		"jwt-decode": "^4.0.0",
		"react-hot-toast": "^2.6.0",
		"tailwind-merge": "^3.4.0",
		"zustand": "^5.0.9"
	}
}